" 配色方案（放在这里的配色后于vimrc中的应用，因此可以实现vim和gvim使用不同配色）
colorscheme molokai~

" 隐藏菜单栏和工具栏
set guioptions=r
map <silent> <F2> :if &guioptions =~# 'T' <Bar>
		\set guioptions-=T <Bar>
		\set guioptions-=m <Bar>
	\else <Bar>
		\set guioptions+=T <Bar>
		\set guioptions+=m <Bar>
	\endif<CR>

" gvim字体
" set gfw=HanaMinB
set guifont=* "monofur\ for\ Powerline\ 12
